# Sector Analysis

Install:

```bash
npx omniskill@latest install sector-analysis
```

Invoke `$sector-analysis` with a market or index, horizon, and available sector evidence. The workflow returns leadership, rotation, relative strength, earnings and policy sensitivity, and limitations for a parent coordinator or standalone user. It uses host-provided browsing and public sources; it requires no API key and does not issue personalized trade advice.
