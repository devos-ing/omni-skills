# Skill Tree Demo

This parent lists only its local entry skill and the direct `../cto` child
workflow. Omniskills scans the child workflow and installs its leaf skills.

```bash
omniskill deps examples/workflows/skill-tree-demo
omniskill lock examples/workflows/skill-tree-demo
omniskill install examples/workflows/skill-tree-demo
```
