{
	"name": "show-me-ur-agents",
	"private": true,
	"type": "module",
	"packageManager": "bun@1.3.8",
	"engines": {
		"bun": ">=1.3.0"
	},
	"workspaces": ["packages/*"],
	"scripts": {
		"preinstall": "bun run ./scripts/enforce-bun.ts",
		"build": "bun run --filter adhdai build",
		"build:server": "bun -e \"console.log('No server workspace package is present yet. Add the ROY-120 server package, then wire this script to its build command.')\"",
		"build:web": "bun run --filter web build",
		"dev:server": "bun -e \"console.log('No server workspace package is present yet. Add the ROY-120 server package, then wire this script to its dev command.')\"",
		"dev:web": "bun run --filter web dev",
		"dev": "bun -e \"console.log('Server/web workspace packages are not present in this branch yet. Use bun run start for the CLI.')\"",
		"start": "bun run ./packages/cli/src/index.ts",
		"smoke": "bun test packages/cli/tests/smoke-flow.test.ts",
		"publish:version": "bun run --filter adhdai publish:version",
		"review:hourly": "bun run --filter adhdai-server review:hourly",
		"test": "bun test packages/cli/tests",
		"typecheck": "bun run --filter adhdai typecheck && bun run --filter web typecheck",
		"format": "bun run biome format --write .",
		"lint": "bun run biome lint .",
		"check": "bun run biome check .",
		"check:write": "bun run biome check --write .",
		"cron:once": "bun run --filter adhdai-server cron:once",
		"review:hourly:once": "bun run --filter adhdai-server review:hourly:once",
		"prepare:publish": "bun run --filter adhdai prepare:publish",
		"changeset": "changeset",
		"prepare": "bun run ./scripts/prepare.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@changesets/cli": "^2.31.0",
		"husky": "^9.1.7"
	}
}
