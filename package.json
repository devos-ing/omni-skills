{
	"name": "piv-loop",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"bin": {
		"piv-loop": "./src/index.ts"
	},
	"scripts": {
		"build": "bun build ./src/index.ts --target bun --outdir dist",
		"start": "bun run ./src/index.ts",
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit",
		"format": "biome format --write .",
		"lint": "biome lint .",
		"check": "biome check .",
		"check:write": "biome check --write ."
	},
	"devDependencies": {
		"@types/bun": "^1.3.2",
		"@biomejs/biome": "1.9.4",
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"pino": "^10.3.1",
		"pino-pretty": "^13.1.3"
	}
}
